SET(PROJ_NAME gw2hack)
PROJECT(${PROJ_NAME})

ADD_LIBRARY(${PROJ_NAME} SHARED main.cpp)

SET_TARGET_PROPERTIES(${PROJ_NAME} PROPERTIES FOLDER ${PROJ_NAME})

TARGET_LINK_LIBRARIES(${PROJ_NAME} hacklib)
