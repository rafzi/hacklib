IF(UNIX)
    RETURN()
ENDIF()


PROJECT(veh_benchmark)

ADD_EXECUTABLE(${PROJECT_NAME} main.cpp)
SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES FOLDER hacklib/examples)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} hacklib)
